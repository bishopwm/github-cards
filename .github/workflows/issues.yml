name: "Issues"
on: issues
jobs:
  issue_updated:
    runs-on: ubuntu-latest
    name: Issue Updated
    steps:
      - name: Update Issues
        uses: fjogeleit/http-request-action@v1.9.1
        with:
          url: "https://peaceful-fairy-c2e727.netlify.app/.netlify/functions/issues"
          method: "POST"
          data: '{"gitHubIssue": ${{ toJSON(github.event.issue) }}}'
